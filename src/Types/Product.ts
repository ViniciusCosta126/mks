/* Declaring a type. */
/* Declaring a type. */
export interface  IProducts{
    id:Number,
    name:String,
    brand:String,
    description:String,
    photo:String,
    price:String
}
export interface ProductProps{
    products:IProducts[]
}
  
//how to declare a type with typescript?
  