/* Declaring a type. */
/* Declaring a type. */
export interface  IProducts{
    id:number,
    name:String,
    brand:String,
    description:String,
    photo:string,
    price:string
}
export interface ProductProps{
    products:IProducts[]
}
  
  